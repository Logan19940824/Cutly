# Web 图片抠图与尺寸修改工具方案

> 调研日期：2026-07-29

## 1. 方案结论

第一版推荐使用 **PhotoRoom Basic API 完成抠图，服务端使用 Sharp/libvips 完成尺寸修改、背景合成和格式转换**。

这样可以快速上线，同时避免针对每种输出尺寸重复调用付费 AI API。月调用量明显增长后，再使用真实业务图片对 PhotoRoom、remove.bg 和国内云厂商进行质量与成本测试。

## 2. API 选型

| 方案 | 适合场景 | 能力与限制 | 建议 |
| --- | --- | --- | --- |
| [PhotoRoom API](https://docs.photoroom.com/) | 快速上线、电商商品图 | Basic 抠图 API 为 `$0.02/次`；Plus 为 `$0.10/次`，可组合定位、阴影、背景和指定尺寸 | 综合首选 |
| [remove.bg API](https://www.remove.bg/api) | 只需要稳定、高质量抠图 | 接口简单；最高 50MP；透明 PNG 最大 10MP，透明 WebP 可到 50MP；每月前 50 次免费 | 纯抠图首选 |
| [Cloudinary AI Background Removal](https://cloudinary.com/documentation/cloudinary_ai_background_removal_addon) | 同时需要上传、存储、CDN、缩放和格式优化 | 可以将抠图、尺寸转换、格式转换和 CDN 组成完整流水线 | 已使用 Cloudinary 时首选 |
| [Clipdrop API](https://clipdrop.co/apis/docs/remove-background) | 已有 Clipdrop/Jasper 合作关系 | 最大 25MP、30MB，支持 PNG、JPEG、WebP；当前文档提示联系 Jasper 获取最新模型和额度 | 新项目暂不优先 |
| [Replicate BRIA](https://replicate.com/bria/remove-background/api) | 快速测试不同模型 | 按运行时间计费，接入灵活，但可能存在冷启动、版本变化和延迟波动 | 原型验证适用 |
| [阿里云通用图像分割](https://help.aliyun.com/zh/viapi/developer-reference/api-segmentation-2019-12-30-segmentcommonimage) | 中国大陆业务、商品或通用物体 | 国内网络、付款与合规更方便；尺寸修改需要自己处理 | 国内部署重点测试 |
| [百度智能云人像分割](https://cloud.baidu.com/doc/BODY/s/Fk3cpyxua) | 证件照、人像应用 | 更偏人像，不适合作为所有商品和复杂物体的唯一方案 | 人像专用备选 |

### PhotoRoom 当前信息

- Remove Background API：`$0.02/次`
- Image Editing API：`$0.10/次`
- Sandbox 提供 1,000 张测试额度
- 基础接口建议不超过 25MP
- 最大文件 50MB，最长边 6,000px
- 输入支持 PNG、JPEG、WebP、HEIC
- 透明输出使用 PNG 或 WebP

价格和额度可能调整，正式上线前应再次检查官方价格页。

## 3. 推荐架构

```text
浏览器
  → 后端获取上传凭证
  → 使用 STS 临时凭证直传阿里云 OSS
  → 后端创建处理任务
  → 抠图 API
  → Sharp 调整尺寸、背景和格式
  → 阿里云 OSS + 阿里云 CDN（可选）
  → 返回预览和下载地址
```

核心原则：

- AI API 只负责抠图。
- 一份透明原图可以生成多个输出规格。
- 前端 Canvas 只负责预览，最终文件由服务端生成。
- 原图和透明抠图结果分开保存，后续改尺寸不重新付费抠图。

## 4. 输出尺寸设计

产品界面应让用户选择尺寸适配模式，而不是只输入宽高。

### contain

完整保留主体，剩余区域透明或填充指定背景色。建议作为默认模式。

### cover

铺满目标画布，允许裁掉部分边缘。

### stretch

强制拉伸到目标宽高，容易导致图片变形，通常不建议提供。

Sharp 示例：

```js
sharp(input)
  .resize(width, height, {
    fit: "contain",
    background: { r: 0, g: 0, b: 0, alpha: 0 }
  })
  .webp({ quality: 90 })
```

可以从同一份透明原图生成：

- 800×800 电商主图
- 1080×1080 社交媒体图片
- 1200×628 广告图
- 证件照尺寸
- 透明 PNG/WebP
- 白底或彩色背景 JPEG

## 5. Web 端功能范围

第一版保留以下最小功能：

1. 上传 JPG、PNG、WebP、HEIC 图片。
2. 自动抠图并展示透明背景预览。
3. 设置目标宽度和高度。
4. 选择 contain 或 cover。
5. 选择透明、白色或自定义背景色。
6. 导出 PNG、WebP 或 JPEG。
7. 下载处理结果。

暂不实现复杂模板系统、批量工作流、自建 AI 模型和自动生成阴影；等真实用户需求出现后再增加。

## 6. 安全与稳定性

- API Key 只能保存在后端，不能写入浏览器代码。
- 校验真实 MIME、扩展名、文件大小和像素数量。
- 限制超大图片，防止图片解压炸弹和内存耗尽。
- 阿里云 OSS Bucket 使用私有权限，浏览器只获取限定路径和时效的 STS 临时凭证。
- 如果允许提交远程图片 URL，必须防止 SSRF；更安全的是只接受上传文件或自有 OSS URL。
- 每个任务设置幂等 ID，避免重试造成重复处理和重复扣费。
- 对 HTTP 429、超时和 5xx 使用指数退避。
- 设置用户级并发、每日额度和单次文件大小限制。
- 使用短期签名 URL 访问原图和结果图。
- 用户删除图片时，同步删除原图、结果图和缓存。
- 上线前确认供应商的数据保留、模型训练、数据区域和隐私条款。

## 7. 成本优化

- 使用图片内容哈希去重，同一原图只调用一次抠图 API。
- 缓存透明抠图结果，修改尺寸时只运行 Sharp。
- 预览图使用较低分辨率，下载时再生成最终分辨率。
- 透明图片优先输出 WebP；需要设计软件兼容或无损交付时提供 PNG。
- 白底和彩色背景图片使用 JPEG，减少存储和 CDN 流量。
- 记录每个任务的供应商、耗时、费用、失败原因和重试次数。
- 设置每日费用告警和硬性费用上限。

## 8. 质量验收

上线前准备 100～300 张真实业务图片进行盲测，至少覆盖：

- 头发和毛发
- 玻璃与半透明物体
- 白色主体和白色背景
- 商品原有阴影
- 复杂和低对比背景
- 小尺寸、压缩严重和模糊图片
- 多主体和主体贴边图片

对每个供应商记录：

- 主体边缘质量
- 半透明区域质量
- 错误删除或背景残留
- 平均响应时间和 P95 响应时间
- 单张实际成本
- 失败率和限流情况

自动抠图无法覆盖所有场景，后续可增加简单蒙版画笔，让用户手动擦除或恢复区域。

## 9. 实施顺序

1. 接入 PhotoRoom Sandbox，完成单图上传和抠图。
2. 使用 Sharp 完成 contain、cover、背景色和格式转换。
3. 接入阿里云 OSS、STS 直传和签名下载地址。
4. 增加任务状态、失败重试、幂等和额度限制。
5. 使用真实样本进行质量和费用测试。
6. 根据测试结果决定是否切换 remove.bg、阿里云或增加备用供应商。

## 10. 最终建议

第一版采用 **PhotoRoom Basic + Sharp + 阿里云 OSS/阿里云 CDN（可选）**。

跳过自建抠图模型和多供应商抽象层；当 API 成本、隐私要求或供应商稳定性真正成为问题时，再增加自托管模型或备用供应商。
